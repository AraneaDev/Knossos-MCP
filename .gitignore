vendor/
/.phpunit.cache/
/.mypy_cache/
/.ruff_cache/
node_modules/
/coverage/
/.knossos/
*.sqlite
*.sqlite-shm
*.sqlite-wal
.php-cs-fixer.cache
.env
__pycache__/
*.pyc

# Rust worker build output; the binary is produced by tools/install or the image build.
/workers/rust/target/
/workers/rust/bin/

# cargo-mutants writes its run output here; it is a local report, not repo content.
/workers/rust/mutants.out/
.claude/settings.local.json
.claude/settings.local.json.*
.superpowers/

# Local-only superpowers specs and plans
/docs/superpowers/

# Generated by Infection; the filename is fixed by the Chaos-MCP integration.
/chaos-infection-log.json

# Written to the project root by Chaos-MCP's per-file Infection runs.
/infection.args.log
/infection.out.log
.audit-patches/

# Audit reports are working artifacts, not repo content
docs/audits/
