// What `svelte-kit sync` generates beside a route, reduced to what the
// fixture reads.
export type LayoutData = { links: { href: string }[] };
export type PageData = Record<string, never>;
export type ActionData = unknown;
